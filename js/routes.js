var routes = {
    index: function(context) {
        /*
        var country = this.params.country
        if(!country) {
            map.getRandomCountry(function(country) {
                context.redirect('#/' + country)
            })
            return;
        }
        map.getMissions(country, function(data) {
            if(!data.rows.length) { // country not found, load a random one
                map.getRandomCountry(function(country) {
                    context.redirect('#/' + country)
                })
            } else { // country FOUND
                map.showMissions(data)
            }
        })
        */
    }
}
